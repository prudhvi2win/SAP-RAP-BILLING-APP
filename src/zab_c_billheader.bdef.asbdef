projection;
strict ( 2 );
use draft;

define behavior for ZAB_C_BillHeader alias BillHeader
{
  use create;
  use update;
  use delete;

  use action Prepare;
  use action Edit;
  use action Resume;
  use action Activate;
  use action Discard;
}